{
  "buildCommand": "cd nyaya-web && npm install && npm run build",
  "outputDirectory": "nyaya-web/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
